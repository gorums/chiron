dist/
# Courses are separate repositories. This is only the default place to clone them.
courses/
__pycache__/
*.pyc
.env
# Reader progress synced from courses served by Studio. Personal, not content.
state/
node_modules/
# Measured with tools that are not part of the platform (see "Metrics"); the database
# is a local artifact of one run, not a number to commit.
.coverage
htmlcov/
